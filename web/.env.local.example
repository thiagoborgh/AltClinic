# API Express (somente server-side — NAO expor ao browser)
EXPRESS_API_URL=http://localhost:3000

# Verificacao JWT (mesma chave do Express)
JWT_SECRET=<copiar do .env do Express>

# URL publica do app (browser pode acessar)
NEXT_PUBLIC_APP_URL=http://localhost:3001
